# 活动位运营 — 完整业务 PRD

## 修订记录

| 修订时间 | 修订内容 | 修订人 |
|------|------|------|
| 2026-05-22 | 初稿 | Kiro |
| 2026-05-22 | 去除审核流程，状态流更新为 draft→active→paused/ended/expired | Kiro |
| 2026-06-24 | 活动地址按区域区分（国内/海外）；新增套餐配置列+套餐配置页（无分类+拖拽排序）；移除活动优先级；新增用户分群管理章节 | Kiro |

---

## 一、业务背景

平台运营在"商城位运营"和"推荐位运营"基础上，需要一个独立的"活动位运营"模块来管理 App 内的营销活动展示。

**与商城位、推荐位的本质区别**：
- **商城位**：长期套餐售卖，无时效限制，重在套餐内容
- **推荐位**：内容/设备推荐，重在个性化匹配
- **活动位**：营销活动推广，有明确的活动周期、首页弹窗展示、活动落地地址

**要解决的痛点**：
- 活动投放散乱，缺乏统一的策略配置管理
- 活动到期后仍展示，引发用户投诉和合规风险
- 无法按区域、APP、用户分群精准投放活动
- 活动效果缺少数据追踪

**产品目标**：
- 提供统一的活动位策略配置后台，支持按区域、APP、用户分群精准投放
- 首页弹窗活动，配置弹窗频率（每N小时1次）
- 活动周期管控，超期自动下线
- 活动状态流转：草稿 → 进行中 → 已暂停/已结束/已过期
- 活动关联套餐配置（无分类、拖拽排序）
- APP 端展示效果与后台配置无缝衔接

---

## 二、名词解释

| 术语 | 说明 |
|------|------|
| 活动位 | App 首页用于展示营销活动的弹窗位置 |
| 活动策略 | 一套完整的活动投放规则：什么时间、向哪些用户、展示什么内容 |
| 活动周期 | 活动生效的时间范围 [startTime, endTime]，超期自动下线 |
| 活动地址 | 用户点击活动后跳转的目标 H5 地址，按区域分为国内地址（domestic）和海外地址（overseas） |
| 套餐配置 | 活动位关联的套餐列表，无分类维度，支持拖拽排序调整展示顺序 |
| 活动状态 | 草稿 → 进行中 / 已暂停 / 已结束 / 已过期 |
| 弹窗频率 | 弹窗展示频率，每N小时弹1次（1-720小时） |
| 用户分群 | 投放的目标用户群体，在全局用户分群管理页「活动位分群」Tab 中维护，共 5 个分群（高价值/新注册/活跃/沉默/付费），编码格式 `ug_activity_xxx` |

---

## 三、业务实体说明

### 3.1 核心实体

**活动策略（ActivityStrategy）**
- 属性：名称、状态（draft/active/paused/ended/expired）、开始/结束时间、投放区域、投放APP、用户分群、活动地址、弹窗频率、备注
- 生命周期：草稿 → 进行中 ⇄ 已暂停 → 已结束/已过期
- 唯一标识：AS_XXXX 格式策略ID

**活动地址（ActivityAddress）**
- 属性：地址名称、H5 URL、所属区域（domestic/overseas）
- 独立管理，策略按投放区域分别选择对应区域的地址

**活动套餐（ActivityPackage）**
- 属性：套餐ID、套餐名称、应用方向、订阅类型、售价、上架状态
- 与活动策略关联，无分类维度，支持拖拽排序

**活动用户分群（ActivityUserGroup）**
- 属性：分群名称、分群编码（`ug_activity_xxx`）、用户数、已绑定策略状态
- 全局统一管理，在「运营管理 → 用户分群 → 活动位分群」Tab 中维护
- 5 个固定分群：高价值用户、活跃用户、新注册用户、沉默用户、付费用户

### 3.2 实体关系

```
活动策略 1──N 活动地址（按区域：国内必选1个国内地址，海外必选1个海外地址）
活动策略 1──N 投放区域（国内/海外，至少选1）
活动策略 1──N 投放APP（牵心PRO/鹤梦之家，至少选1）
活动策略 1──N 用户分群（5个分群，至少选1）
活动策略 1──N 活动套餐（无分类，拖拽排序）
```

---

## 四、核心业务流程

### 4.1 活动策略全生命周期

```mermaid
flowchart LR
    A[1. 创建<br/>填写配置] --> B[2. 发布<br/>状态→进行中]
    B --> C[3. 运行<br/>用户可见]
    C --> D[4. 结束<br/>自动/手动]
    C --> E[手动暂停]
    E --> C
```

### 4.2 3步创建流程

```
Step 1: 基础信息
  → 活动名称（必填，最长50字）
  → 活动周期（必填，结束>开始）
  → 备注（选填）

Step 2: 投放配置
  → 弹窗频率（必填，1-720小时）
  → 投放区域（多选，至少选1）
  → 投放APP（多选，至少选1）
  → 投放用户分群（多选，至少选1）

Step 3: 活动内容
  → 根据投放区域动态显示地址选择器：
    · 投放「国内」→ 显示国内活动地址下拉（必填，仅列出国内地址）
    · 投放「海外」→ 显示海外活动地址下拉（必填，仅列出海外地址）
  → 提交 → 状态为"草稿"；发布 → 状态变为"进行中"
```

### 4.3 活动状态流转

```mermaid
stateDiagram-v2
    [*] --> draft: 创建活动
    draft --> active: 发布
    draft --> draft: 保存草稿
    active --> paused: 手动暂停
    active --> expired: 到期自动
    active --> ended: 手动结束
    paused --> active: 恢复
    paused --> ended: 结束
    expired --> [*]
    ended --> [*]
```

### 4.4 全局协作时序图

活动从策划到上线的全链路，涉及运营、UI设计、H5开发、后台配置、APP 展示五个角色：

```mermaid
sequenceDiagram
    participant OP as 运营
    participant UI as UI设计
    participant H5 as H5开发
    participant Admin as 管理后台
    participant APP as APP端

    OP->>OP: 策划活动方案<br/>(目标/人群/周期/预算)
    OP->>UI: 提出设计需求
    UI->>UI: 设计活动视觉稿<br/>(弹窗素材/落地页)
    UI-->>OP: 交付设计稿

    OP->>H5: 提供设计稿+活动规则
    H5->>H5: 开发活动H5落地页<br/>(套餐绑定/购买流程)
    H5-->>OP: H5地址交付

    OP->>Admin: 登录管理后台
    Admin->>Admin: 配置活动地址<br/>(名称+H5 URL)
    OP->>Admin: 创建活动策略<br/>Step1: 名称+周期<br/>Step2: 频率+区域+APP+分群<br/>Step3: 选择活动地址
    Admin-->>OP: 策略保存(草稿)
    OP->>Admin: 发布活动
    Admin->>Admin: 状态→进行中

    APP->>Admin: 拉取活动列表
    Admin-->>APP: 返回有效活动
    APP->>APP: 校验有效期+频率
    APP-->>OP: 首页弹窗展示
    OP->>APP: 查看效果数据
```

### 4.5 APP 端展示流程（时序图）

```mermaid
sequenceDiagram
    participant U as 用户
    participant A as APP
    participant S as 后台
    participant LS as localStorage

    U->>A: 打开App进入首页
    A->>S: GET 活动列表(status=active)
    S-->>A: 返回有效活动列表
    A->>A: 双重校验有效期（本地+服务器时间）
    A->>A: 按创建时间取第一条
    A->>LS: 检查dismiss缓存
    LS-->>A: 返回上次关闭时间
    alt 间隔已到 & 同会话未弹
        A-->>U: 延迟500ms弹出弹窗
        U->>A: 点击"立即查看"
        A->>A: 跳转活动H5地址
        A->>LS: 记录dismiss时间
    else 间隔未到 或 已dismiss
        A-->>U: 不弹窗
    end
```

---

## 五、业务规则

### 5.1 策略管理

| 编号 | 规则 | 说明 |
|------|------|------|
| R01 | 名称必填 | 活动名称不能为空，最长50字 |
| R02 | 周期合法 | 活动结束时间必须晚于开始时间 |
| R03 | 频率必填 | 弹窗频率必须设置（1-720小时） |
| R04 | 区域必选 | 每个策略至少选择一个投放区域 |
| R05 | APP必选 | 每个策略至少选择一个投放APP |
| R06 | 分群必选 | 每个策略至少选择一个用户分群 |
| R07 | 地址按区域必选 | 按投放区域分别校验：投放国内则国内地址必填，投放海外则海外地址必填 |
| R08 | 冲突排序 | 同一APP+区域冲突时按创建时间排序 |
| R09 | 终态不可编辑 | 已结束/已过期的活动不可编辑，只能复制新建 |
| R10 | 进行中不可删除 | 进行中活动需先暂停或结束后才能删除 |
| R11 | 软删除 | 删除活动为软删除，保留记录 |

### 5.2 APP 端展示

| 编号 | 规则 | 说明 |
|------|------|------|
| R12 | 仅拉取有效 | 仅拉取 status=active 且在 [startTime, endTime] 范围内的活动 |
| R13 | 双重校验 | 客户端本地时间与服务器时间双重校验，防止时区偏差 |
| R14 | 关闭缓存 | 弹窗关闭记录存储在 localStorage，key: `carecam_activity_dismiss` |
| R15 | 频率控制 | 距上次弹窗超过 popupInterval 小时才再次弹窗 |
| R16 | API降级 | API 不可用时使用前端内置 Mock 数据 |

---

## 六、功能架构

```
活动位运营系统
├── 管理后台（iot-platform）
│   ├── 活动策略列表
│   │   ├── 筛选栏（状态/区域/APP + 搜索）
│   │   ├── 策略表格（名称/周期/状态/频率/套餐配置/操作）
│   │   └── 状态批量筛选
│   ├── 添加/编辑策略（3步抽屉）
│   │   ├── Step1: 基础信息
│   │   ├── Step2: 投放配置
│   │   └── Step3: 按区域选择活动地址
│   ├── 查看活动详情（抽屉·只读）
│   ├── 活动地址配置（二级页面）
│   │   └── 地址CRUD列表（含区域列）
│   ├── 活动套餐配置（二级页面）
│   │   └── 套餐列表（拖拽排序 + 添加/移除）
│   ├── 用户分群管理（全局页面）
│   │   └── 活动位分群 Tab（5个分群 + 用户数 + 绑定策略）
│   └── 活动操作（按状态动态显示）
│       ├── 发布 / 编辑 / 删除
│       ├── 暂停 / 恢复 / 结束
│       └── 复制新建
│
└── APP 端（carecam_pro）
    ├── ActivityPopup — 首页弹窗（含频率控制）
    └── ActivityDemoPanel — 演示控制面板
```

---

## 七、详细功能描述

### 7.1 活动策略列表

#### 功能应用场景
运营管理员查看所有活动策略，了解当前投放状态，进行筛选和管理操作。

#### 列表展示
- 每行显示：活动名称+ID、活动周期、状态标签、弹窗频率、套餐配置（N个套餐，点击跳转）、备注摘要、操作按钮
- 支持按状态/区域/APP筛选
- 支持按活动名称搜索
- 列表按创建时间倒序排列

#### 操作按钮（按状态动态变化）

| 状态 | 可用操作 |
|------|----------|
| 草稿 | 编辑 + 删除 + 发布 |
| 进行中 | 查看 + 暂停 |
| 已暂停 | 查看 + 恢复 + 结束 |
| 已结束 | 查看 + 复制新建 |
| 已过期 | 查看 + 复制新建 |

---

### 7.2 创建/编辑活动策略

#### 功能应用场景
运营管理员创建新的活动投放策略，或编辑已有的草稿策略。

#### Step 1: 基础信息
- 活动名称：文本输入，最长50字符，必填
- 活动周期：日期时间范围选择器，必填，结束时间必须晚于开始时间
- 备注：文本域，选填

#### Step 2: 投放配置
- 弹窗频率：数字输入，范围 1-720 小时，默认24
- 投放区域：复选框（国内/海外），至少选1
- 投放APP：复选框（牵心PRO/鹤梦之家），至少选1
- 投放用户分群：复选框（5个分群），至少选1

#### Step 3: 选择活动地址
- 根据 Step 2 选择的投放区域，动态显示对应的地址选择器：
  - 投放「国内」→ 显示「国内活动地址」下拉（必填），仅列出 region=domestic 的地址
  - 投放「海外」→ 显示「海外活动地址」下拉（必填），仅列出 region=overseas 的地址
  - 国内+海外同时投放 → 两个下拉均显示，分别选择
- 选中后显示地址名称+URL预览
- 保存草稿：校验通过后保存，状态为"草稿"
- 发布：校验通过后状态变为"进行中"

#### 编辑限制
- 已结束/已过期的策略不可编辑，只能复制新建
- 进行中的策略不可编辑

---

### 7.3 查看活动详情

#### 功能应用场景
运营管理员查看进行中/已结束/已过期活动策略的完整信息。

#### 详情抽屉
- 只读展示策略全部信息（名称、周期、区域、APP、分群、频率、地址）
- 包含操作按钮（按状态动态显示）

---

### 7.4 活动地址配置

#### 功能应用场景
运营管理员管理活动H5地址列表，供策略创建时选择。

#### 功能要点
- 独立页面：地址列表表格（名称 + 区域标签 + H5地址 + 操作）
- 区域标签：国内（primary）/ 海外（warning）
- 添加/编辑弹窗：地址名称 + 所属区域（国内/海外）+ URL
- 删除前确认提示
- 已关联到策略的地址删除时提示"已被N个策略引用"

---

### 7.5 活动套餐配置

#### 功能应用场景
运营管理员为活动策略关联套餐，用户在活动弹窗中可查看和购买套餐。

#### 功能要点
- 入口：活动策略列表中「套餐配置」列点击套餐数量 → 跳转套餐配置页
- 页面结构：返回按钮 + 活动名称标题 + 套餐列表表格
- 套餐列表表格列：拖拽手柄、排序号、套餐信息（名称+ID）、应用方向、订阅类型、售价、上架状态（switch）、移除
- **无分类**：套餐为扁平列表，不区分云存储/AI/综合等分类
- **拖拽排序**：拖拽左侧手柄可调整套餐展示顺序
- **添加套餐**：弹窗展示可选套餐列表（表格+复选框+筛选），已添加的套餐自动排除
- 筛选条件：投放区域、投放APP、搜索套餐名称或ID
- 已选计数 + 确定添加

### 7.6 APP 端 — 首页弹窗

#### 功能应用场景
用户打开 App 时看到活动弹窗，引导参与限时活动。

#### 功能要点
- 仅展示 status=active 且在有效期内的活动
- 按创建时间取最新的一条
- 延迟 500ms 弹出（避免与页面加载动画冲突）
- 弹窗内容：活动名称 + "立即查看"按钮 + "暂不参与"
- 频率控制：距上次弹窗超过 popupInterval 小时才弹
- 关闭记录存储在 localStorage
- 同一会话只弹一次

### 7.7 用户分群管理

#### 功能应用场景
运营管理员在全局用户分群页查看和管理活动位使用的 5 个用户分群，了解各分群用户数量和绑定策略情况。

#### 功能要点
- 入口：左侧导航「运营管理 → 用户分群」→ 切换到「活动位分群」Tab
- 分群表格列：分群名称、分群编码（`ug_activity_xxx`）、用户数量、已绑定策略状态
- 点击分群行可查看绑定了该分群的策略列表（抽屉展示）
- 分群由系统预定义，不支持新增或删除（固定 5 个分群）
- 分群编码格式：`ug_activity_{name}`，全局唯一

#### 分群明细

| 分群名称 | 编码 | 用户数 | 说明 |
|----------|------|--------|------|
| 高价值用户 | `ug_activity_high_value` | 12,580 | 消费金额/频次高于平均水平的用户 |
| 活跃用户 | `ug_activity_active` | 45,230 | 近 30 天有设备操作行为的用户 |
| 新注册用户 | `ug_activity_new_reg` | 18,920 | 近 7 天内注册的新用户 |
| 沉默用户 | `ug_activity_silent` | 6,180 | 近 60 天无设备操作行为的用户 |
| 付费用户 | `ug_activity_paid` | 3,450 | 有付费套餐订阅记录的用户 |

---

## 八、页面信息架构

### 8.1 页面层级（管理后台）

```
iot-platform
└── 运营管理
    └── 活动位运营（策略列表页）
        ├── 添加活动抽屉（3步）
        ├── 编辑活动抽屉（3步）
        ├── 查看详情抽屉（只读）
        ├── 活动地址配置页
        │   └── 地址CRUD列表（含区域）
        ├── 活动套餐配置页
        │   └── 套餐列表（拖拽排序 + 添加/移除）
        └── 用户分群页（全局）
            └── 活动位分群 Tab（5个分群 + 绑定策略查询）
```

### 8.2 页面层级（APP 端）

```
carecam_pro
└── 首页（home/index.vue）
    ├── ActivityPopup（弹窗）
    └── ActivityDemoPanel（演示控制）
```

### 8.3 页面跳转关系

| 起点 | 触发 | 终点 |
|------|------|------|
| 左侧导航「活动位运营」 | 点击 | 策略列表页 |
| 策略列表「添加活动」 | 点击 | 添加抽屉（当前页） |
| 列表行「编辑」 | 点击 | 编辑抽屉（当前页） |
| 列表行「查看」 | 点击 | 详情抽屉（当前页） |
| 策略列表「套餐配置」 | 点击套餐数量 | 活动套餐配置页 |
| 活动地址配置 tab | 点击 | 地址配置页 |
| 左侧导航「用户分群」 | 点击 → 活动位分群 Tab | 用户分群页 |
| APP 弹窗「立即查看」 | 点击 | 外部 H5 |
| APP 弹窗「暂不参与」 | 点击 | 关闭弹窗 |

---

## 九、异常说明

| 分类 | 场景 | 处理方式 |
|------|------|------|
| 数据异常 | 活动周期重叠 | 前端提示冲突列表，确认后仍可提交 |
| 数据异常 | 活动已过期仍展示 | 后端定时任务 + 客户端本地时间双校验 |
| 数据异常 | 发布时必填项缺失 | 前端校验，阻止发布并定位到对应步骤 |
| 数据异常 | 活动地址被删除 | 已关联策略仍保留地址信息，新策略不可选 |
| 网络异常 | API 不可用 | APP 降级使用前端内置 Mock 数据 |
| 操作异常 | 已结束活动被编辑 | 前端置灰编辑按钮，后端校验拦截 |
| 操作异常 | 进行中活动被删除 | 前端置灰删除按钮，后端校验拦截 |
| 极限场景 | 多活动冲突 | 按创建时间排序，前端提示 |
| 边界条件 | 活动周期跨年 | 正常支持，时间范围无年份限制 |
| 边界条件 | 弹窗 dismiss 缓存在 localStorage | 清理 APP 缓存后重置，属于预期行为 |

---

> **本文档为纯业务PRD，面向产品和开发团队。不包含API路由路径、数据库建表语句等技术实现细节。**
