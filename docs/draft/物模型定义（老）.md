# 物模型定义（老）

> 老版本设备配置项，来源于 `camercfg.db`、`devcfg.db`、`iniotcfg.db` 三份旧配置文件。这些设备未进行标准化能力定义，但平台需向下兼容，故保留全部配置项。
>
> **组织方式**：按模块组织，每个模块下定义原子能力。标注 `per Device` 表示整机唯一，标注 `per CamId` 表示多目设备每个镜头独立一份。标识符沿用原始字段名（PascalCase），值类型均为字符串（与设备上报格式保持一致）。

```json
{
  // ═══════════════════════════════════════════════
  // 一、设备信息（per Device，devcfg.db）
  // ═══════════════════════════════════════════════
  "DeviceInfoModule": {
    "Sign": "206511",                 // 配置签名/版本号
    "DevModel": "YH-F10PM1602",     // 设备型号
    "DevName": "鸟",                  // 设备名称
    "DevVersion": "YGT.YH-F10PM1602-v01.60.230221.1519", // 固件版本
    "SdkVersion": "50855936",         // SDK版本号（十进制）
    "DevType": "1",                 // 设备类型
    "OsType": "6",                   // 操作系统类型
    "OldDevFlag": "0",              // 老设备标志：0=新设备，1=老设备
    "Language": "0",                // 设备语言
    "PromptSet": "0"                // 提示音设置
  },

  // ═══════════════════════════════════════════════
  // 二、网络与配网（per Device，devcfg.db）
  // ═══════════════════════════════════════════════
  "NetAccessModule": {
    "WifiSetAbility": "25",          // WiFi设置能力位掩码
    "DualFrequency": "0",            // 双频能力：0=仅2.4G，1=双频
    "WifiStrength": "0",            // WiFi信号强度
    "SSID": "SZ-HuiYunTEK",         // WiFi SSID
    "MacAddr": "60:1D:9D:F5:FF:EE", // MAC地址

    // AP热点
    "ApSetPasswd": "0",             // AP密码设置能力：0=不支持，1=支持
    "ApHasPassWd": "0",             // AP是否有密码：0=无，1=有

    // 多媒体连接
    "MulMedia": {
      "Port": "25035",              // 媒体端口
      "Ipv4": "223.4.176.210",      // P2P/服务器IPv4
      "ReSource": "0123456789abcdef1300016400002444", // 资源标识
      "EncType": "49",             // 加密类型
      "EncKey": "48109eb2cc0e7678", // 加密密钥
      "EncLv": "a9899293e9bc2ea7"   // 加密级别
    },

    // 会话管理
    "MaxSessionCnt": "4",            // 最大会话数
    "AliveSessionCnt": "0"          // 当前活跃会话数
  },

  // ═══════════════════════════════════════════════
  // 三、SIM/4G模块（per Device，devcfg.db）
  // ═══════════════════════════════════════════════
  "SimModule": {
    "SIMAbility": "0",                // SIM卡能力：0=不支持，1=支持
    "SimSlotCnt": "0",               // SIM卡槽数量
    "WorkSimSlotId": "0",            // 当前工作SIM卡槽ID
    "IMEI": "",                       // 4G模组IMEI
    "SimSlots": []                    // SIM卡槽列表
  },

  // ═══════════════════════════════════════════════
  // 四、电源与功耗（per Device，devcfg.db / iniotcfg.db）
  // ═══════════════════════════════════════════════
  "PowerModule": {
    "EnergyType": "1",               // 供电方式：0=电池，1=常电，2=电池+太阳能
    "Powersupply": "1",             // 供电类型
    "PowerLevel": "0",              // 电量档位
    "SupplyShowPowerAbility": "1",  // 显示电量能力：0=不支持，1=支持

    // 唤醒能力
    "AwakeAbility": "2",            // 唤醒方式：0=不支持，1=本地，2=远程，3=本地+远程
    "AwakeInterval": "0",           // 唤醒间隔（秒）

    // AOV模式
    "AOVAbility": "0",               // AOV能力：0=不支持，1=支持
    "SwitchAOVFrameRateAbility": "0", // AOV帧率切换能力：0=不支持，1=支持
    "AOVFrameInterval": "0"          // AOV帧间隔（秒）：0=智能
  },

  // ═══════════════════════════════════════════════
  // 五、OTA升级（per Device，devcfg.db）
  // ═══════════════════════════════════════════════
  "OTAModule": {
    "OTAAbility": "7",               // OTA能力位掩码
    "OTAType": "0"                  // OTA类型
  },

  // ═══════════════════════════════════════════════
  // 六、加密模块（per Device，devcfg.db）
  // ═══════════════════════════════════════════════
  "EncryptionModule": {
    "EncAbility": "3"                // 加密能力位掩码
  },

  // ═══════════════════════════════════════════════
  // 七、时区与同步（per Device，devcfg.db + iniotcfg.db IoTS 1093）
  // ═══════════════════════════════════════════════
  "TimeModule": {
    "DstArea": "Asia/Shanghai",      // 时区
    "Dst": "CST-8",                  // 夏令时偏移
    "DstUseFlag": "1",              // 夏令时启用：0=关，1=开
    "TimeMode": "0",                // 时间模式
    "AutoSync": "1",                // 自动同步：0=关，1=开
    "TimeArea": "",                  // 时区范围（iniotcfg.db 1093）
    "TimeZone": ""                   // 时区（iniotcfg.db 1093）
  },

  // ═══════════════════════════════════════════════
  // 八、AI侦测能力声明（per Device，iniotcfg.db 顶层）
  // 整机级能力声明；具体算法配置在 IoTS 服务中
  // ═══════════════════════════════════════════════
  "AIDetectionModule": {
    "MotionAbility": "1",             // 运动侦测能力：0=不支持，1=支持
    "PIRAbility": "0",               // PIR侦测能力：0=不支持，1=支持
    "DoorbellAbility": "0",          // 门铃能力：0=不支持，1=支持
    "CameraAbility": "1",           // 摄像机能力：0=不支持，1=支持
    "EventAbility": "1",            // 事件能力：0=不支持，1=支持
    "RecordAbility": "1",            // 录制能力：0=不支持，1=支持
    "SnapshotAbility": "1",          // 抓图能力：0=不支持，1=支持
    "VoiceAlarmAbility": "0",        // 语音报警能力：0=不支持，1=支持
    "BuzzerAbility": "1",           // 蜂鸣器能力：0=不支持，1=支持
    "SupportCloudFlag": "1",         // 支持云服务：0=不支持，1=支持
    "CloudRecordAbility": "1",       // 云端录制能力：0=不支持，1=支持
    "CloudSnapshotAbility": "1",     // 云端抓图能力：0=不支持，1=支持
    "CamWorkType": "0"               // 摄像机工作类型：0=普通
  },
  
  // ═══════════════════════════════════════════════
  // 十、视频管理（per CamId）
  // 画面相关原子能力：镜头、码流、图像、OSD、隐私遮蔽、PTZ
  // ═══════════════════════════════════════════════
  "VideoModule": {
    "CamId": "0",                     // 摄像机ID
    "CamStatus": "1",                 // 摄像机开关：0=关，1=开

    // ── 镜头/传感器 ──
    "Lens": {
      "MaxLenCount": "1",           // 最大镜头数
      "CurrentLenID": "0",         // 当前镜头ID
      "AutoFlag": "1",              // 自动镜头标志：0=手动，1=自动
      "DefaultLenId": "0",         // 默认镜头ID
      "RectifyAbility": "0",       // 畸变校正能力：0=不支持，1=支持
      "ZoomAbility": "0",          // 变焦能力：0=不支持，1=支持
      "CurZoomValue": 0,           // 当前变焦值
      "CombineFlag": 0,            // 双镜拼接标志
      "Lenses": [
        {
          "LenID": "0",            // 镜头ID
          "LenName": "0",         // 镜头名称
          "LensType": "0",        // 镜头类型
          "LenFocalLenth": "4",   // 焦距（mm）
          "MinLenFocalLenth": "0",
          "MaxLenFocalLenth": "0"
        }
      ]
    },

    // ── 码流与编码 ──
    "Stream": {
      "StreamCount": "2",           // 码流数量（如主流+子流）
      "Streamers": [
        {
          "StreamID": "0",         // 码流ID：0=主流，1=子流
          "ResolutionAbility": "64",
          "Resolution": "64",      // 当前分辨率
          "Width": "2304",         // 画面宽度
          "Height": "1296",        // 画面高度
          "EncAbility": "6",      // 编码能力位掩码
          "EncType": "4",         // 编码类型：4=H.265
          "HideFlag": "0",        // 隐藏标志
          "SmartEncFlag": "0",    // 智能编码标志
          "Quality": "1",         // 画质档位
          "FrameRate": "15",      // 帧率（fps）
          "BiteRate": "8",        // 码率档位
          "RateType": "0",        // 码率类型
          "FrameInterval": "30"   // 帧间隔
        },
        {
          "StreamID": "1",         // 子流
          "ResolutionAbility": "0", "Resolution": "0",
          "Width": "848", "Height": "480",
          "EncAbility": "6", "EncType": "4",
          "HideFlag": "0", "SmartEncFlag": "0",
          "Quality": "1", "FrameRate": "15",
          "BiteRate": "5", "RateType": "0", "FrameInterval": "30"
        }
      ]
    },

    // ── 图像画面 ──
    "Image": {
      "IRRedAbility": "1",          // 红外灯能力：0=不支持，1=支持
      "IRRedMode": "0",             // 红外模式：0=自动，1=手动，2=强制
      "FullColorMode": "0",        // 全彩模式：0=不支持，1=支持

      "MirrorAbility": "0",        // 镜像能力：0=不支持，1=支持
      "MirrorType": "0",           // 镜像类型：0=正常，1=水平
      "InversionAbility": "1",     // 翻转能力：0=不支持，1=支持
      "InversionType": "1",        // 翻转类型：0=正常，1=180°

      "WDRAbility": "1",           // WDR宽动态能力：0=不支持，1=支持
      "WDRMode": "0",              // WDR模式
      "HDRAbility": "0",           // HDR能力：0=不支持，1=支持
      "HDRMode": "0",              // HDR模式

      "AntiFlickerAbility": "0",   // 抗频闪能力：0=不支持，1=支持
      "ScanFrequery": "0",        // 扫描频率：0=50Hz，1=60Hz

      "PIPAblity": "0"             // 画中画能力：0=不支持，1=支持
    },

    // ── OSD水印 ──
    "OSD": {
      "OSDAblity": "1",            // OSD能力：0=不支持，1=支持
      "OSDSetAbility": "1",       // OSD设置能力：0=不支持，1=支持
      "OSDName": "",               // OSD显示名称
      "OSDPosition": "0",         // OSD位置
      "OSDCustomSwitch": "0",     // 自定义OSD开关：0=关，1=开
      "OSDCommonSwitch": "0"      // 公共OSD开关：0=关，1=开
    },

    // ── 隐私遮蔽 ──
    "PrivacyMask": {
      "PrivacyMaskAbility": "0",   // 隐私遮蔽能力：0=不支持，1=支持
      "HumanRgnAblity": "0",      // 人形区域检测能力：0=不支持，1=支持
      "PrivacyList": []             // 遮蔽区域列表
    },

    // ── PTZ云台 ──
    "PTZ": {
      "PTZMode": "3",               // 云台模式：0=无云台，3=全功能
      "PTZAbility": "0",           // 云台能力：0=不支持，1=支持
      "AttachPTZAbility": "0",     // 外接云台能力：0=不支持，1=支持
      "PTZSpeedAbility": "0"       // 云台速度调节能力：0=不支持，1=支持
    }
  },

  // ═══════════════════════════════════════════════
  // 十一、音频管理（per Device）
  // 麦克风能力及音频编码参数
  // ═══════════════════════════════════════════════
  "AudioModule": {
    "MicroPhoneAbility": "1",        // 麦克风能力：0=不支持，1=支持
    "MicroPhoneStatus": "1",         // 麦克风状态：0=关，1=开
    "MicVolumeAbility": "0",        // 麦克风音量调节能力：0=不支持，1=支持
    "AudioParam": {
      "EncType": "2",               // 音频编码类型
      "SampleRate": "8000",          // 采样率（Hz）
      "Channel": "1",               // 声道数
      "Depth": "16"                  // 位深度
    }
  },

  // ═══════════════════════════════════════════════
  // 十二、喇叭管理（per Device）
  // 语音播放、音量、铃声
  // ═══════════════════════════════════════════════
  "SpeakerModule": {
    "VoicePlayAbility": "2",        // 语音播放能力：0=不支持，1=半双工，2=全双工
    "Volumn": "50",                  // 音量，0-100
    "PlayMode": "1",                 // 播放模式
    "RingToneSetAbility": "0",     // 铃声设置能力：0=不支持，1=支持
    "RingToneDecAbility": "7"      // 铃声解码能力位掩码
  },

  // ═══════════════════════════════════════════════
  // 十三、存储管理（per CamId）
  // 事件录制、云存储、本地SD卡存储
  // ═══════════════════════════════════════════════
  "StorageModule": {
    "CamId": "0",
    "StorageAbility": "1",           // 存储能力：0=不支持，1=支持
    "StorageStatus": "0",           // 存储状态：0=正常，1=异常

    "EventRecord": {
      "OpenFlag": "1",              // 服务开关：0=关，1=开
      "RecordLoop": "1",            // 循环录制：0=关，1=开
      "RecordFull": "0",            // 满盘处理：0=覆盖，1=停止
      "StreamID": "0",              // 录制码流ID
      "Duration": "15"              // 录制时长（秒）
    },

    "CloudStorage": {
      "OpenFlag": "1",              // 服务开关
      "Status": "1",                // 状态
      "Support": "1",               // 是否支持
      "GifOpenFlag": "1",           // GIF动态图开关
      "Interval": "30",             // 上传间隔（秒）
      "LoopDays": "30",             // 循环天数
      "PicType": "1",              // 图片类型
      "RecordType": "1",            // 录制类型
      "SnapJpgCnt": "3",            // 抓图数量
      "StorageType": "0",           // 存储类型
      "hm_editable": "0"            // 是否可编辑
    },

    "LocalStorage": {
      "OpenFlag": "1",              // 服务开关
      "Duration": 30,               // 录制时长（秒）
      "LoopDays": "3",              // 循环天数
      "RecordType": "2",            // 录制类型
      "StorageAbility": 2,          // 存储能力
      "StreamID": "1"              // 录制码流ID
    }
  },

  // ═══════════════════════════════════════════════
  // 十四、状态灯（per CamId）
  // ═══════════════════════════════════════════════
  "IndicatorModule": {
    "CamId": "0",
    "StatusLedMode": "1",           // 状态灯模式：0=自适应，1=始终关闭
    "StatusLampAbility": "0",       // 状态灯能力：0=不支持，1=支持
    "LampAbility": "0"             // 白光灯能力：0=不支持，1=支持
  }
}
```

---

*创建日期: 2026-06-05 | 基于 camercfg.db / devcfg.db / iniotcfg.db 整理 | 共 14 个模块（11 个 per Device + 3 个 per CamId）*
